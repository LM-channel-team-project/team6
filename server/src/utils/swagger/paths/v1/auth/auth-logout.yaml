get:
  summary: "유저 로그아웃"
  tags:
    - Auth
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: number
                example: 200
              message:
                type: string
                example: 로그아웃 성공
    400:
      $ref: "../../../swagger.yaml#/components/responses/BadRequest"
